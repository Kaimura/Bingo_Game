export const columnBingoCheck = (tilesColumnArray) => {
    
}

export const rowBingoCheck = (tilesRowArray) => {
    // const allChecked = tilesRowArray.every((element) => element.checked); // access checked state of tiles to determine if full row is Bingo...
}

export const diagonalBingoCheck = (tilesDiagonalArray) => {
    
}