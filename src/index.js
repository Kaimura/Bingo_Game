import React from 'react';
import ReactDOM from 'react-dom';
import BingoBoard from './molecules/BingoBoard/BingoBoard';

ReactDOM.render(
  <React.StrictMode>
    <BingoBoard />
  </React.StrictMode>,
  document.getElementById('root')
);